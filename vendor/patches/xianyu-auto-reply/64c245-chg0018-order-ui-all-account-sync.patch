diff --git a/frontend/src/pages/orders/Orders.tsx b/frontend/src/pages/orders/Orders.tsx
index 8e4025677f18542adf8d4cb375bd9c8cb20d323d..0e4d26d7218d826956d77cdbc762a2579ccad8fc 100644
--- a/frontend/src/pages/orders/Orders.tsx
+++ b/frontend/src/pages/orders/Orders.tsx
@@ -285,5 +285,2 @@ export function Orders() {
-    // 未选择账号时给出提示，不再默认禁用按钮
-    if (!selectedAccount) {
-      addToast({ type: 'warning', message: '请先选择账号' })
-      return
-    }
+    // 不选择账号时沿用 Backend 原生语义：同步当前用户下全部启用账号。
+    // 选择账号仅用于需要时的单账号同步，不再作为获取订单的前置条件。
diff --git a/tests/test_chg0018_order_sold_permission_header.py b/tests/test_chg0018_order_sold_permission_header.py
index cbbfe2eeda095db9821c85e3eb5585c78802e420..f7725975f5703d77f027e619f32a3588588ab378 100644
--- a/tests/test_chg0018_order_sold_permission_header.py
+++ b/tests/test_chg0018_order_sold_permission_header.py
@@ -25,0 +26,16 @@ def test_refund_order_request_is_not_changed_by_sold_order_fix() -> None:
+
+
+def test_orders_ui_does_not_require_account_before_sync() -> None:
+    source = (ROOT / "frontend/src/pages/orders/Orders.tsx").read_text(encoding="utf-8")
+    handler = _block(source, "const handleFetchXianyuOrders", "// 勾选操作")
+    assert "请先选择账号" not in handler
+    assert "fetchXianyuOrders(selectedAccount || undefined)" in handler
+
+
+def test_backend_fetch_orders_supports_all_active_accounts_when_account_omitted() -> None:
+    source = (ROOT / "backend-web/app/api/routes/orders.py").read_text(encoding="utf-8")
+    handler = _block(source, '@router.post("/fetch-xianyu")', '@router.get("/{order_no}")')
+    assert "cookie_id: str | None = None" in source
+    assert "if request.cookie_id:" in handler
+    assert "accounts = await account_service.list_accounts(owner_id)" in handler
+    assert "accounts = [acc for acc in accounts if (acc.status or \"active\") not in inactive_statuses]" in handler
